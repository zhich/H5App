<resources>

    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@android:color/transparent</item>
        <item name="android:buttonStyle">@style/custombtn_style_app</item>
        <!-- <item name="android:windowBackground">@drawable/main_bg</item> -->
        <item name="android:windowBackground">@color/bg_color</item>
    </style>

    <style name="custombtn_style_app" parent="@android:style/Widget.Button">
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:textColor">@color/pay_tras</item>
        <item name="android:paddingLeft">10dip</item>
        <item name="android:paddingRight">10dip</item>
        <item name="android:paddingTop">3dip</item>
        <item name="android:paddingBottom">3dip</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!--   全屏幕拉伸   -->
    <style name="layout_full">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>
    <!--   包裹自身大小   -->
    <style name="layout_wrap">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <!--   横向分布   -->
    <style name="layout_horizontal" parent="layout_full">
        <item name="android:layout_width">0dip</item>
    </style>
    <!--   纵向分布   -->
    <style name="layout_vertical" parent="layout_full">
        <item name="android:layout_height">0dip</item>
    </style>

    <style name="titlebar_title">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_alignParentLeft">true</item>
        <item name="android:drawableLeft">@drawable/titlebar_back</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingLeft">3dp</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:background">@drawable/seletor_titlebar_item_bg</item>
        <item name="android:clickable">true</item>
    </style>
</resources>
